import React from 'react';
import TextSearch from '../TextSearch';

describe('<TextSearch />', () => {
    // it('renders input text', () => {
    //     const wrapper = shallow(<TextSearch />);
    //     expect(wrapper.find('input').length).toBe(1);
    // });

    // it('onChange callback does not fire on input change', () => {
    //     const changeCallback = jest.fn();
    //     const wrapper = shallow(<TextSearch onChange={changeCallback} />);
    //     wrapper.find('input').simulate('change', { target: { value: 'asdf' } });
    //     expect(changeCallback).not.toBeCalled();
    // });

    // it('onChange callback fires on blur change', () => {
    //     const submitCallback = jest.fn();
    //     const wrapper = shallow(<TextSearch onChange={submitCallback} />);
    //     wrapper.find('input').simulate('blur', { target: { value: 'asdf' } });
    //     expect(submitCallback).toBeCalled();
    //     expect(submitCallback).toBeCalledWith('asdf');
    // });

    // it('onChange callback fires on enter key press', () => {
    //     const submitCallback = jest.fn();
    //     const wrapper = shallow(<TextSearch onChange={submitCallback} />);
    //     wrapper.find('input').simulate('keyPress', { key: 'Enter', target: { value: 'asdf' } });
    //     expect(submitCallback).toBeCalled();
    //     expect(submitCallback).toBeCalledWith('asdf');
    // });
});

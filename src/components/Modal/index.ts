import Modal, { ModalProps } from './Modal';
import ConfirmationModal, { ConfirmationModalProps } from './ConfirmationModal';

export { ConfirmationModal, ConfirmationModalProps, ModalProps };
export default Modal;
